# 论文导读PPT生成器 - 产品需求文档 (PRD)

## 1. 产品概述

### 1.1 产品名称
论文导读PPT生成器 (Paper Guide Generator)

### 1.2 产品定位
一款面向科研人员和学生的AI驱动工具，能够自动解析学术论文PDF，生成结构化导读大纲，并根据用户选择的汇报场景智能生成PPT内容和导读文章。

### 1.3 核心价值
- **节省时间**：将数小时的论文阅读和PPT制作压缩到分钟级别
- **结构化理解**：AI辅助提取论文核心贡献、方法、实验等关键信息
- **场景适配**：针对不同汇报场景（组会、课程、开题、跨领域）生成风格各异的内容
- **信息完整**：PPT内容详实，把事情讲清楚，把数据摆明白

---

## 2. 用户角色

| 角色 | 描述 | 核心需求 |
|------|------|----------|
| 研究生 | 需要在组会汇报论文 | 快速理解论文，制作批判性分析PPT |
| 本科生 | 课程作业需要讲解论文 | 用通俗语言解释复杂概念 |
| 博士生 | 开题/综述需要梳理文献 | 对比方法演进，分析研究趋势 |
| 跨领域研究者 | 向非专业听众介绍研究 | 避免术语，强调研究意义 |

---

## 3. 页面设计与交互

### 3.1 首页 (Index)

#### 页面布局
```
┌─────────────────────────────────────────────────────────────────┐
│  Header: Logo + 产品名称                                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │ 项目卡片 1  │  │ 项目卡片 2  │  │ 项目卡片 3  │  ...        │
│  │ 论文标题    │  │ 论文标题    │  │ 论文标题    │             │
│  │ 模板 · 页数 │  │ 模板 · 页数 │  │ 模板 · 页数 │             │
│  │ 更新时间    │  │ 更新时间    │  │ 更新时间    │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  + 新建项目（虚线边框卡片）                                 ││
│  │    点击上传论文 PDF 开始                                    ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 视觉设计
- **背景**：渐变色 `bg-gradient-to-br from-background via-background to-primary/5`
- **卡片**：白色卡片，hover 时上移 + 阴影加深
- **状态指示**：不同 step 显示不同颜色的徽章

#### 交互逻辑
| 操作 | 结果 |
|------|------|
| 点击项目卡片 | 根据项目 `step` 跳转：`outline`→大纲页，`template`→模板页，`workspace/done`→工作台 |
| 点击"新建项目" | 跳转 `/upload` |
| Hover 卡片右上角 | 显示删除按钮 |

---

### 3.2 上传页面 (UploadPage)

#### 页面布局
```
┌─────────────────────────────────────────────────────────────────┐
│  ← 返回                                     步骤指示：1 → 2 → 3 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│              ┌───────────────────────────────────┐              │
│              │                                   │              │
│              │    📄                             │              │
│              │                                   │              │
│              │  拖放 PDF 文件到此处              │              │
│              │  或 点击上传                      │              │
│              │                                   │              │
│              │  支持 .pdf 格式，最大 50MB        │              │
│              │                                   │              │
│              └───────────────────────────────────┘              │
│                                                                 │
│              ┌───────────────────────────────────┐              │
│              │ ✓ 正在提取论文文本…               │  (解析中)    │
│              │ ○ 正在识别论文结构…               │              │
│              │ ○ 正在提取核心贡献…               │              │
│              │ ○ 正在分析方法与实验…             │              │
│              │ ○ 正在生成导读大纲…               │              │
│              └───────────────────────────────────┘              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 视觉设计
- **上传区域**：虚线边框，hover 时高亮
- **拖拽状态**：边框变为 primary 色，背景半透明
- **解析进度**：步骤列表，当前步骤有 spinner 动画

#### 交互逻辑
| 操作 | 结果 |
|------|------|
| 拖入 PDF | 高亮上传区 → 开始提取文本 |
| 点击上传区 | 打开文件选择器 |
| 文本提取完成 | 自动调用 AI 解析 |
| AI 解析完成 | 自动跳转到大纲编辑页 |
| 解析失败 | 显示错误 Toast，允许重试 |

---

### 3.3 大纲编辑页面 (OutlinePage)

#### 页面布局
```
┌─────────────────────────────────────────────────────────────────────────────┐
│  ← 返回    论文标题                           [收起PDF] [下一步: 选择模板 →] │
├────────────────────────────────┬────────────────────────────────────────────┤
│                                │                                            │
│   📄 论文原文 (PDF Viewer)     │   大纲编辑器                               │
│                                │                                            │
│   ┌──────────────────────────┐ │   ▼ 1. 引言                                │
│   │                          │ │      核心贡献与研究动机                    │
│   │                          │ │                                            │
│   │   (iframe 渲染 PDF)       │ │   ▼ 2. 相关工作                            │
│   │                          │ │      现有方法的局限性                      │
│   │                          │ │      ├─ 2.1 传统方法                       │
│   │                          │ │      └─ 2.2 深度学习方法                   │
│   │                          │ │                                            │
│   │   独立滚动                │ │   ▼ 3. 方法                                │
│   │                          │ │      本文提出的方法                        │
│   │                          │ │      ├─ 3.1 整体架构                       │
│   │                          │ │      ├─ 3.2 核心模块                       │
│   │                          │ │      └─ 3.3 训练策略                       │
│   │                          │ │                                            │
│   └──────────────────────────┘ │   ▼ 4. 实验                                │
│                                │      ...                                   │
│   宽度: 400px，可折叠          │                                            │
│                                │   独立滚动                                 │
├────────────────────────────────┴────────────────────────────────────────────┤
│                           [下一步: 选择模板 →]                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 视觉设计
- **PDF 面板**：左侧固定宽度，灰色边框分隔
- **大纲节点**：卡片式设计，hover 时显示操作按钮
- **层级缩进**：每级缩进 24px，连接线指示层级关系
- **当前编辑**：双击进入编辑态，输入框替换文本

#### 交互逻辑
| 操作 | 结果 |
|------|------|
| 双击节点标题 | 进入编辑模式，显示输入框 |
| 双击节点描述 | 进入描述编辑模式 |
| 点击 ⋮ 菜单 | 显示操作下拉：添加子节点、删除、置顶、上移、下移、置底 |
| 点击折叠图标 | 折叠/展开子节点 |
| 点击"收起PDF" | 左侧 PDF 面板收起（动画） |
| 点击"下一步" | 保存大纲 → 跳转模板选择页 |

#### 节点操作菜单
```
┌────────────────┐
│ + 添加子节点   │
├────────────────┤
│ ⬆ 置顶        │
│ ↑ 上移        │
│ ↓ 下移        │
│ ⬇ 置底        │
├────────────────┤
│ 🗑 删除        │
└────────────────┘
```

---

### 3.4 模板选择页面 (TemplatePage)

#### 页面布局
```
┌─────────────────────────────────────────────────────────────────────────────┐
│  ← 返回    选择汇报模板                                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐  ┌─────────┐│
│  │ 🎓 组会汇报版   │  │ 📚 课程版       │  │ 📋 开题综述版  │  │ 🌐 跨方向││
│  │                 │  │                 │  │                 │  │         ││
│  │ ┌─────────────┐ │  │ ┌─────────────┐ │  │ ┌─────────────┐ │  │ ...     ││
│  │ │ PPT 预览    │ │  │ │ PPT 预览    │ │  │ │ PPT 预览    │ │  │         ││
│  │ │ (单页示例)  │ │  │ │ (单页示例)  │ │  │ │ (单页示例)  │ │  │         ││
│  │ │             │ │  │ │             │ │  │ │             │ │  │         ││
│  │ └─────────────┘ │  │ └─────────────┘ │  │ └─────────────┘ │  │         ││
│  │                 │  │                 │  │                 │  │         ││
│  │ 批判性分析     │  │ 通俗易懂        │  │ 研究脉络       │  │ 去术语  ││
│  │ 实验设计合理性 │  │ 类比解释概念    │  │ 方法演进对比   │  │ 强调意义││
│  │                 │  │                 │  │                 │  │         ││
│  │ [选择此模板]   │  │ [选择此模板]   │  │ [选择此模板]   │  │ [选择]  ││
│  └─────────────────┘  └─────────────────┘  └─────────────────┘  └─────────┘│
│                                                                             │
│  ────────────────────────────────────────────────────────────────────────── │
│                                                                             │
│  内容密度：  ○ 简洁    ● 标准（推荐）    ○ 详细                            │
│                                                                             │
│                           [开始生成 →]                                      │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 视觉设计
- **模板卡片**：2x2 网格布局，选中时边框高亮 + 阴影
- **PPT 预览**：16:9 比例，内置 Transformer 介绍示例
- **密度选项**：Radio Group，位于底部

#### 四种模板预览内容（均以"什么是Transformer"为主题）

| 模板 | 预览 PPT 标题 | 预览内容风格 |
|------|--------------|--------------|
| 组会汇报版 | Self-Attention 机制分析 | 计算复杂度 O(n²)、对比实验数据 |
| 课程版 | 注意力机制原理 | "Self-Attention 就像开会讨论"，类比解释 |
| 开题综述版 | 序列建模演进 | RNN → LSTM → Transformer 时间线 |
| 跨方向版 | 为什么 Transformer 重要 | 应用场景、影响力，避免技术细节 |

#### 交互逻辑
| 操作 | 结果 |
|------|------|
| 点击模板卡片 | 选中该模板，边框高亮 |
| 选择密度选项 | 更新密度设置 |
| 点击"开始生成" | 保存配置 → 跳转工作台 → 触发 AI 生成 |

---

### 3.5 工作台页面 (WorkspacePage)

#### 页面布局
```
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│  ← 返回   论文标题                              [原文] [导读] [导出]                    │
├──────────────┬──────────────┬──────────────────────────────────────┬────────────────────┤
│              │              │                                      │                    │
│  📄 论文原文 │  📖 导读文章  │        PPT 编辑区域                   │  📑 页面缩略图    │
│              │              │                                      │                    │
│  ┌──────────┐│  ▼ 引言      │   ┌────────────────────────────────┐ │  ┌──────────────┐ │
│  │          ││    第一段…   │   │                                │ │  │ 1. 封面      │ │
│  │  PDF     ││    第二段…   │   │   Transformer 架构解析          │ │  └──────────────┘ │
│  │  Viewer  ││              │   │                                │ │  ┌──────────────┐ │
│  │          ││  ▼ 方法      │   │   • Self-Attention 机制        │ │  │ 2. 引言 ✓    │ │
│  │  (iframe)││    段落…     │   │   • 多头注意力                  │ │  └──────────────┘ │
│  │          ││    段落…     │   │   • 位置编码                    │ │  ┌──────────────┐ │
│  │          ││              │   │                                │ │  │ 3. 方法      │ │
│  │  独立滚动││  点击段落    │   └────────────────────────────────┘ │  └──────────────┘ │
│  │          ││  跳转到对应  │                                      │  ┌──────────────┐ │
│  │          ││  的PPT页面   │   ┌────────────────────────────────┐ │  │ 4. 实验      │ │
│  │          ││              │   │ ✨ 对第 2 页提出修改意见…       │ │  └──────────────┘ │
│  │          ││              │   │                          [发送] │ │        ...       │
│  │          ││              │   └────────────────────────────────┘ │  ┌──────────────┐ │
│  └──────────┘│              │                                      │  │ + 新增页面   │ │
│              │              │                                      │  └──────────────┘ │
│  宽度:360px  │  宽度:300px  │               flex-1                 │    宽度:208px     │
│  可折叠      │  可折叠      │                                      │                    │
├──────────────┴──────────────┴──────────────────────────────────────┴────────────────────┤
│  演讲笔记 ▼                                     简洁 | 自然 | 正式 | 课堂风  🔄 📋     │
│  ┌──────────────────────┬──────────────────────┬──────────────────────┐                 │
│  │ 这页讲什么            │ 补充说明             │ 过渡句               │                 │
│  │ 介绍注意力机制的核心… │ 可以提及 Query/Key…  │ 接下来我们看具体实现…│                 │
│  └──────────────────────┴──────────────────────┴──────────────────────┘                 │
└─────────────────────────────────────────────────────────────────────────────────────────┘
```

#### 区域说明

| 区域 | 宽度 | 功能 | 可折叠 |
|------|------|------|--------|
| PDF 原文 | 360px | 显示用户上传的论文原文，独立滚动 | ✓ |
| 导读文章 | 300px | 显示生成的导读文章，点击段落跳转PPT | ✓ |
| PPT 编辑区 | flex-1 | 当前页面预览 + AI 修改输入框 | ✗ |
| 页面缩略图 | 208px | 所有页面列表，点击切换 | ✗ |
| 演讲笔记 | 100% | 底部抽屉，三栏笔记 + 语气切换 | ✓ |

#### 视觉设计
- **PPT 预览**：卡片式，圆角 + 阴影，16:9 比例
- **AI 输入框**：底部，带 ✨ 图标，支持 Shift+Enter 换行
- **缩略图**：选中时 primary 边框，hover 显示拖拽手柄
- **笔记抽屉**：折叠时只显示标题栏

#### PPT 编辑区 - 布局渲染

不同 `layout` 类型的渲染方式：

```
title-points (默认)          title-two-column              title-quad
┌────────────────────┐       ┌────────────────────┐       ┌────────────────────┐
│ 标题               │       │ 标题               │       │ 标题               │
├────────────────────┤       ├─────────┬──────────┤       ├─────────┬──────────┤
│ • 要点1            │       │ 优点    │ 缺点     │       │ 模块1   │ 模块2    │
│ • 要点2            │       │ • ...   │ • ...    │       │ 说明... │ 说明...  │
│ • 要点3            │       │ • ...   │ • ...    │       ├─────────┼──────────┤
│                    │       │         │          │       │ 模块3   │ 模块4    │
└────────────────────┘       └─────────┴──────────┘       │ 说明... │ 说明...  │
                                                          └─────────┴──────────┘

title-timeline                title-findings
┌────────────────────┐       ┌────────────────────┐
│ 标题               │       │ 标题               │
├────────────────────┤       ├────────────────────┤
│ ●─ 2017: RNN       │       │ ┌────────────────┐ │
│ │                  │       │ │ 🔍 核心发现1   │ │
│ ●─ 2018: LSTM      │       │ └────────────────┘ │
│ │                  │       │ ┌────────────────┐ │
│ ●─ 2019: Transformer│      │ │ 🔍 核心发现2   │ │
│                    │       │ └────────────────┘ │
└────────────────────┘       └────────────────────┘
```

#### 交互逻辑

| 操作 | 结果 |
|------|------|
| 点击缩略图 | 切换到对应页面 |
| 点击导读文章段落 | 跳转到关联的 PPT 页面 |
| 在 AI 输入框输入 + 发送 | 调用 slide-ai 修改当前页 |
| 点击 PPT 右上角 ⋮ | 显示快捷操作：精简内容、更适合讲解、换排版、生成笔记 |
| 切换语气选项 | 更新当前页笔记语气 |
| 点击 🔄 | 重新生成演讲笔记 |
| 点击 📋 | 复制演讲笔记到剪贴板 |
| 点击"+ 新增页面" | 在末尾添加空白页 |
| 点击"原文"按钮 | 切换 PDF 面板显示/隐藏 |
| 点击"导读"按钮 | 切换导读文章面板显示/隐藏 |

#### AI 修改指令示例
```
输入: "请精简这一页的内容，只保留最核心的要点"
输入: "请让这一页的内容更适合口头讲解，语言更自然"
输入: "请换一种排版方式，比如改用分栏布局"
输入: "请为这一页生成演讲注释"
```

---

## 4. 功能需求详细

### 4.1 数据存储

#### localStorage
- **项目列表** (`paper-guide-projects`)：`[{id, title, template, slideCount, updatedAt, step}]`
- **当前项目** (`current_project`)：完整项目数据

#### IndexedDB
- **数据库名**：`paper-guide-db`
- **表名**：`pdfs`
- **记录**：`{id: 'current', blob: Blob, name: string}`

### 4.2 AI 生成逻辑（核心！）

**输入**
- 用户编辑后的大纲（作为 prompt 骨架）
- 用户选择的模板
- 用户选择的内容密度
- 论文元信息（标题、作者、摘要等）

**输出**
- `slides[]`：PPT 幻灯片数组
- `article`：导读文章

**生成规则**
1. **以大纲为骨架**：大纲的每个一级节点对应PPT的一个章节
2. **不强制一章一页**：根据信息量决定页数，信息量大的章节可拆成2-3页
3. **内容要充实**：终极要求是把事情讲清楚，把数据摆明白
4. **善用布局格式**：根据内容类型选择合适的布局

### 4.3 数据模型

#### PaperMeta
```typescript
interface PaperMeta {
  title: string;
  authors: string[];
  year: number;
  keywords: string[];
  topic: string;
  abstract?: string;
}
```

#### OutlineNode
```typescript
interface OutlineNode {
  id: string;
  parentId: string | null;
  level: number;
  title: string;
  description: string;
  order: number;
  children: OutlineNode[];
  collapsed?: boolean;
  isPage?: boolean;
}
```

#### SlideNotes
```typescript
interface SlideNotes {
  mainTalk: string;        // 这页要讲什么
  extraExplanation: string; // 补充说明
  transitionSentence: string; // 过渡到下一页的衔接语
  tone: 'concise' | 'natural' | 'formal' | 'classroom';
}
```

### 4.4 PPT 布局类型

| Layout | 用途 | 适用场景 |
|--------|------|----------|
| `cover` | 封面页 | 论文标题、作者信息 |
| `title-points` | 标题+要点列表 | 最常用，一般内容 |
| `title-subpoints` | 标题+要点+子要点 | 有层级的内容 |
| `title-two-column` | 双栏对比 | 优缺点对比、方法对比 |
| `title-findings` | 核心发现 | 突出重要结论 |
| `title-summary` | 总结页 | 章节/全文总结 |
| `title-quad` | 四分框布局 | 4个并列概念/组件介绍 |
| `title-timeline` | 时间线布局 | 研究脉络、方法演进 |
| `title-method-flow` | 方法流程图 | 方法步骤说明 |
| `title-results` | 结果展示 | 数据对比、实验结果 |

### 4.5 内容块类型

| Type | 用途 |
|------|------|
| `point` | 普通要点 |
| `subpoint` | 子要点（缩进显示） |
| `finding` | 核心发现（高亮显示） |
| `summary` | 总结性文字 |
| `text` | 普通文本 |
| `heading` | 小标题 |
| `quad-item` | 四分框中的一项 |
| `timeline-item` | 时间线中的一个节点 |

---

## 5. 页面流程

```
首页 (/index)
    │
    ├── 新建项目 → 上传页面 (/upload)
    │                   │
    │                   ▼
    │              大纲编辑 (/outline)
    │                   │
    │                   ▼
    │              模板选择 (/template)
    │                   │
    │                   ▼
    │              工作台 (/workspace)
    │
    └── 恢复项目 → 根据 step 跳转对应页面
```

---

## 6. 项目进度追踪

| Step | 状态 | 页面 |
|------|------|------|
| `outline` | 大纲编辑中 | /outline |
| `template` | 模板选择中 | /template |
| `workspace` | 工作台编辑中 | /workspace |
| `done` | 已完成 | /workspace |

---

## 8. 未来迭代方向

### 8.1 导出功能
- 导出为 PPTX 文件（使用 pptxgenjs）
- 导出为 PDF 讲义
- 导出为 Markdown 文档

### 8.2 协作功能
- 用户登录
- 项目云端存储
- 多人协作编辑

### 8.3 AI 增强
- 配图生成（根据内容自动配图）
- 多语言支持
- 个性化风格学习

### 8.4 更多场景
- 会议汇报模板
- 项目答辩模板
- 技术分享模板

---

## 9. 附录

### 9.1 Edge Function 配置
```toml
[functions.paper-parse]
verify_jwt = false

[functions.workspace-generate]
verify_jwt = false
```

### 9.2 关键依赖
- pdfjs-dist: PDF 文本提取
- framer-motion: 动画效果
- sonner: Toast 通知
- lucide-react: 图标库
